import { useState, useEffect } from 'react';
import { useAuth } from '../context/AuthContext';
import { Dice6, Check, RefreshCw } from 'lucide-react';
const ch = {uz:["10 daq kitob o'qi","50 ta otjimaniya","5 ta so'z yodla","Do'stga qo'ng'iroq","30 daq yuring","8 stakan suv","Ertaga reja tuzing","10 daq meditatsiya","Yangi odat boshlang","Tartibga soling","Birovga yordam","Yangi retsept","15 daq cho'zish","Yutuqlarni yozing","1 soat telefonsiz"],ru:["10 мин чтения","50 отжиманий","5 новых слов","Позвоните другу","30 мин прогулки","8 стаканов воды","План на завтра","10 мин медитации","Новая привычка","Наведите порядок","Помогите кому-то","Новый рецепт","15 мин растяжки","Запишите успехи","1 час без телефона"],en:["Read 10 min","50 push-ups","Learn 5 words","Call a friend","Walk 30 min","8 glasses water","Plan tomorrow","10 min meditation","New habit","Organize","Help someone","New recipe","15 min stretch","Write wins","1hr phone-free"]};
export default function DailyChallenge() { const { language, addPoints } = useAuth(); const lang = language||'uz'; const today = new Date().toISOString().split('T')[0]; const [state, setState] = useState(() => { const s = localStorage.getItem('flowly-challenge'); return s ? JSON.parse(s) : {date:'',idx:0,done:false}; }); useEffect(() => { if(state.date!==today){const idx=Math.floor(Math.random()*ch[lang].length);setState({date:today,idx,done:false});} }, [today]); useEffect(() => { localStorage.setItem('flowly-challenge', JSON.stringify(state)); }, [state]); const complete = () => { setState(s=>({...s,done:true})); addPoints(5); }; const refresh = () => { setState({date:today,idx:Math.floor(Math.random()*ch[lang].length),done:false}); }; return (<div className="max-w-lg mx-auto space-y-6 text-center"><h1 className="text-2xl font-bold" style={{color:'var(--text-primary)'}}>🎲 {lang==='ru'?'Челлендж дня':lang==='en'?'Daily Challenge':'Kunlik Challenge'}</h1><div className="card py-10" style={{background:'linear-gradient(135deg, rgba(59,130,246,0.05), rgba(139,92,246,0.05))'}}><Dice6 size={48} className="text-blue-500 mx-auto mb-4"/><p className="text-xl font-bold px-4" style={{color:'var(--text-primary)'}}>"{ch[lang][state.idx]}"</p></div>{state.done?<div className="card py-6 bg-green-50 dark:bg-green-900/10"><Check size={32} className="text-green-500 mx-auto mb-2"/><p className="text-green-600 font-bold">{lang==='ru'?'Выполнено! +5':lang==='en'?'Done! +5':'Bajardim! +5'} ⭐</p></div>:<div className="flex gap-3"><button onClick={complete} className="flex-1 py-4 rounded-2xl bg-gradient-to-r from-green-500 to-emerald-500 text-white font-bold shadow-2xl active:scale-95">✅ {lang==='ru'?'Готово':'Done!'}</button><button onClick={refresh} className="p-4 rounded-2xl" style={{background:'var(--bg-secondary)',border:'1px solid var(--border)'}}><RefreshCw size={20} style={{color:'var(--text-secondary)'}}/></button></div>}</div>); }
