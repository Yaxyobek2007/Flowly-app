import { useState, useEffect, useRef } from 'react';
import { useAuth } from '../context/AuthContext';
import { Play, Pause } from 'lucide-react';
export default function Meditation() { const { language } = useAuth(); const lang = language||'uz'; const [duration, setDuration] = useState(5); const [timeLeft, setTimeLeft] = useState(0); const [active, setActive] = useState(false); const [sessions, setSessions] = useState(()=>parseInt(localStorage.getItem('flowly-med-sessions')||'0')); const ref = useRef(null); useEffect(() => { if(active&&timeLeft>0){ref.current=setInterval(()=>setTimeLeft(t=>t-1),1000);}else if(timeLeft===0&&active){setActive(false);setSessions(s=>{const n=s+1;localStorage.setItem('flowly-med-sessions',n.toString());return n;});if('vibrate' in navigator)navigator.vibrate([300,100,300]);} return()=>clearInterval(ref.current); },[active,timeLeft]); const start=()=>{setTimeLeft(duration*60);setActive(true);}; const m=Math.floor(timeLeft/60);const s=timeLeft%60; return(<div className="max-w-lg mx-auto space-y-6 text-center"><h1 className="text-2xl font-bold" style={{color:'var(--text-primary)'}}>🧘 {lang==='ru'?'Медитация':lang==='en'?'Meditation':'Meditatsiya'}</h1>{!active?<div className="card space-y-6 py-8"><div className="text-6xl animate-pulse">🧘</div><div className="flex gap-2 justify-center">{[3,5,10,15,20].map(t=>(<button key={t} onClick={()=>setDuration(t)} className={`px-4 py-2 rounded-xl text-sm font-bold ${duration===t?'bg-purple-500 text-white shadow-lg':''}`} style={duration!==t?{background:'var(--bg-secondary)',color:'var(--text-secondary)'}:{}}>{t} {lang==='ru'?'мин':'min'}</button>))}</div><button onClick={start} className="w-full py-4 rounded-2xl bg-gradient-to-r from-purple-500 to-indigo-500 text-white font-bold text-lg shadow-2xl active:scale-95"><Play size={24} className="inline mr-2"/>{lang==='ru'?'Начать':'Boshlash'}</button></div>:<div className="card py-12" style={{background:'linear-gradient(180deg, rgba(139,92,246,0.05), rgba(79,70,229,0.1))'}}><div className="text-5xl font-bold font-mono mb-4" style={{color:'var(--text-primary)'}}>{String(m).padStart(2,'0')}:{String(s).padStart(2,'0')}</div><p className="text-sm animate-pulse mb-6" style={{color:'var(--accent)'}}>{lang==='ru'?'Дышите...':lang==='en'?'Breathe...':'Nafas oling...'}</p><div className="w-16 h-16 mx-auto rounded-full bg-purple-500/20 animate-ping"></div><button onClick={()=>{setActive(false);setTimeLeft(0);}} className="mt-8 px-6 py-2 rounded-xl bg-red-500/20 text-red-500"><Pause size={16} className="inline mr-1"/>Stop</button></div>}<div className="card text-center"><p className="text-2xl font-bold text-purple-500">{sessions}</p><p className="text-xs" style={{color:'var(--text-secondary)'}}>{lang==='ru'?'Сессий':'Sessions'}</p></div></div>); }
